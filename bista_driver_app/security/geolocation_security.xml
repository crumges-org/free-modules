<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="geolocation_history_admin_rule" model="ir.rule">
            <field name="name">Geolocation History: Admin Record Rule</field>
            <field name="model_id" ref="model_geolocation_history"/>
            <field name="domain_force">['|',('company_id','=',False),('company_id', 'in', company_ids)]</field>
            <field name="groups" eval="[(4, ref('base.group_system')), (4, ref('base.group_erp_manager'))]"/>
        </record>
    </data>
</odoo>
