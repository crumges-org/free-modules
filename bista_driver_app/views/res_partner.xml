<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
     <record id="contacts.action_contacts" model="ir.actions.act_window">
        <field name="domain">[('is_driver', '=', False)]</field>
    </record>

    <record id="view_partner_form_inherit_fleet_api" model="ir.ui.view">
        <field name="name">view.partner.form.inherit.fleet.api</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='industry_id']" position="before">
                <field name="is_driver" groups="base.group_system"/>
            </xpath>
        </field>
    </record>

    <record id="contact_dispatcher_view_tree" model="ir.ui.view">
        <field name="name">contact.dispatcher.view.list</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <list string="Dispatcher">
                <field name="complete_name" string="Name"/>
                <field name="phone" class="o_force_ltr" optional="show"/>
                <field name="mobile" optional="hide"/>
                <field name="email" optional="show"/>
                <field name="city" optional="show"/>
                <field name="state_id" optional="hide" readonly="1"/>
                <field name="country_id" optional="show" readonly="1"/>
            </list>
        </field>
    </record>

</odoo>