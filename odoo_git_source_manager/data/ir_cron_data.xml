<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Scheduled Action: Update All Enabled Workspaces -->
        <record id="ir_cron_git_update_all" model="ir.cron">
            <field name="name">Git: Update All Enabled Workspaces</field>
            <field name="model_id" ref="model_git_workspace" />
            <field name="state">code</field>
            <field name="code">model.cron_update_all_workspaces()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active" eval="True" />
            <field name="user_id" ref="base.user_admin" />
        </record>

        <!-- Scheduled Action: Cleanup Old Logs -->
        <record id="ir_cron_git_cleanup_logs" model="ir.cron">
            <field name="name">Git: Cleanup Old Update Logs</field>
            <field name="model_id" ref="model_git_update_log" />
            <field name="state">code</field>
            <field name="code">model._cleanup_old_logs(days=90)</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True" />
            <field name="user_id" ref="base.user_admin" />
            <field name="nextcall"
                eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 02:00:00')" />
        </record>

    </data>
</odoo>