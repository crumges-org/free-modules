<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="ir_cron_automation_campaigns" model="ir.cron">
        <field name="name">Run AI Call Campaigns</field>
        <field name="model_id" ref="ai_caller_agent.model_automation_campaign"/>
        <field name="state">code</field>
        <field name="code">model._cron_run_automation_campaigns()</field>

        <field name="interval_number">5</field>
        <field name="interval_type">minutes</field>

        <field name="user_id" ref="base.user_root"/>
        <field name="active">True</field>
    </record>
</odoo>
